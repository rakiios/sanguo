name: Check Character Documents

on:
  pull_request:
    paths:
      - 'characters/*.md'

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate character documents
        run: python scripts/check_characters.py
